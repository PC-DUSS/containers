Dockerfile to build a local PHP dev environment

"launch" is used to run the container for the first time using Podman

Subsequently, use "podman start" to start the container if it is ever stopped

Contains the config file for PHP-FPM "www.conf" to be used inside the container
